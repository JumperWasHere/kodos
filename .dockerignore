# Build artifacts
.next
out

# Dependencies (rebuilt inside Docker)
node_modules

# Secrets — never bake into the image
.env
.env.local
.env*.local

# Dev / editor
.git
.gitignore
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
README.md

# Docker itself
Dockerfile
.dockerignore
